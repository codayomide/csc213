opposite = float(input('Input Opposite side: '))
adjacent = float(input('Input Adjacent side: '))

hypotenus_sqr = (opposite ** 2) + (adjacent ** 2)
hypotenus = hypotenus_sqr ** 0.5
print("The Length of the hypotenus is:", hypotenus) 